/**
 * kotlin에는 4가지의 접근 제어자가 있다.
 * private, public -> java와 동일
 * protected -> 자식 클래스가 접근이 가능함
 * internal -> 같은 모듈 안에서 접근 가능
 *
 * kotlin에서 클래스의 속성과 메서드는 public이 기본이다
 */

class Car (val yearOfMake : Int){
//    private var fuelLevel = 100
//    private get // 이런식으로 getter나 setter에 접근제어자 설정 가능
}


